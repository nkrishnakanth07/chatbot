services:
  # Backend API Service
  - type: web
    name: chatbot-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false  # Set this in Render dashboard
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    
  # Frontend (Static Site)
  - type: web
    name: chatbot-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: build
    envVars:
      - key: REACT_APP_API_URL
        value: https://chatbot-backend.onrender.com  # Update with your backend URL
    plan: free
    autoDeploy: true
    
# Optional: Background worker for document processing
# Uncomment if you want async processing
# - type: worker
#   name: chatbot-worker
#   runtime: python
#   rootDir: backend
#   buildCommand: pip install -r requirements.txt
#   startCommand: python worker.py
#   envVars:
#     - key: OPENAI_API_KEY
#       sync: false
#   plan: free
