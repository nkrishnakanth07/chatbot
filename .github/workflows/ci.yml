name: CI - Tests and Linting

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master ]

jobs:
  backend-tests:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3
      
      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov flake8
      
      - name: 🔍 Lint with flake8
        run: |
          # Stop build if there are Python syntax errors
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          # Treat all warnings as errors (optional)
          # flake8 . --count --max-complexity=10 --max-line-length=127 --statistics
      
      - name: 🧪 Run tests
        run: |
          echo "No tests configured yet - add pytest tests in backend/tests/"
          # pytest backend/tests/ --cov=backend --cov-report=xml
      
      - name: ✅ Dependency check
        run: |
          python -c "import fastapi; print('✓ FastAPI')"
          python -c "import langchain; print('✓ LangChain')"
          python -c "import openai; print('✓ OpenAI')"
          echo "All dependencies installed successfully!"
  
  frontend-tests:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3
      
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - name: 📦 Install dependencies
        run: |
          cd frontend
          npm ci
      
      - name: 🧪 Run tests
        run: |
          cd frontend
          # npm test -- --coverage --watchAll=false
          echo "No tests configured yet - add tests in frontend/src/"
      
      - name: 🏗️ Build check
        run: |
          cd frontend
          npm run build
        env:
          CI: false
      
      - name: ✅ Build successful
        run: |
          echo "Frontend built successfully!"
  
  security-scan:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3
      
      - name: 🔒 Run security scan
        run: |
          echo "Security scan placeholder"
          # Add npm audit, pip audit, etc.
          # npm audit --audit-level=high
          # pip-audit
